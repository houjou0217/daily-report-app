# 日報生成アプリ 設計書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 日報生成アプリ 設計書 |
| バージョン | 1.2 (Codex向け実装規約反映) |
| 作成日 | 2026-08-11 |
| 対応要件定義書 | 01_要件定義書.md |

---

## 2. システム構成

### 2.1 全体構成
完全ローカル完結のスタンドアロン・デスクトップアプリとする。サーバー、外部API、クラウド連携は一切持たない。

```
┌─────────────────────────────┐
│        デスクトップアプリ        │
│                              │
│  [UI層]                      │
│   ・案件管理画面               │
│   ・日報入力画面(作業ブロック方式) │
│   ・プレビュー/コピー画面        │
│                              │
│  [処理層]                     │
│   ・整形ロジック(時系列ソート)     │
│   ・入力バリデーション          │
│                              │
│  [データ層]                    │
│   ・ローカルファイル(JSON)      │
└─────────────────────────────┘
         │
         ▼（通信なし）
   ローカルディスクのみ
```

### 2.2 技術スタック（確定）

本プロジェクトでは以下を採用する。

| 項目 | 採用 |
|---|---|
| デスクトップ | Electron |
| 言語 | TypeScript |
| ビルド/配布 | Electron Forge |
| バンドラ | Webpack |
| UI | Vanilla HTML / CSS / TypeScript |
| テスト | Vitest |
| パッケージ管理 | npm |
| 開発Node.js | Node.js 24 LTS |
| 配布形式 | Forge ZIP maker |

UI規模と完全ローカル要件を踏まえ、React/Vue等のUIフレームワークおよびCSSフレームワークは採用しない。
不要な依存を減らし、main / preload / renderer / domain / data の責務分離を優先する。

Electron ForgeのVite連携は採用せず、first-partyのTypeScript + Webpackテンプレートを基準とする。
Phase00の具体的な初期化・scripts・security設定は `05_Phase00実装仕様.md` を正とする。


### 2.3 通信遮断の担保方法
- `package.json` の依存関係から、通信を行うライブラリ（analytics, crash-reporter, auto-updater 等）を含めない
- Electronの `autoUpdater` 機能を使用しない
- renderer / preload / main のいずれにも外部URLへの `fetch` / `axios` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` 等の通信コードを置かない
- HTML/CSSから Google Fonts、CDN、外部画像等のリモートリソースを読み込まない。フォントはOS標準フォントまたはアプリに同梱したローカル資産のみを使用する
- Content Security Policyはローカル資産のみを許可する方向で設定し、外部オリジンへの接続を許可しない
- ビルド後、ネットワークを遮断した環境で動作確認を行う（後述 8章）
- コードレビュー時にURL文字列・通信API・通信系依存パッケージを静的検索する

### 2.4 Electronプロセス境界・IPC方針
- renderer からNode.js APIやファイルシステムへ直接アクセスさせない
- `contextIsolation: true`、`nodeIntegration: false`、`sandbox: true` を前提とする
- ローカルファイルの読み書き、保存先解決、クリップボード操作等のOS依存処理は main 側へ集約する
- preload では `contextBridge` を用い、画面が必要とする最小限のAPIだけを公開する
- IPCのチャンネル名と引数は固定し、任意コマンド実行・任意ファイルパスアクセスにつながる汎用APIを作らない
- renderer から受け取るデータは main 側でも検証してから保存する

### 2.5 実装ディレクトリ構成（Codex向け基準）
```text
/
├─ AGENTS.md
├─ 01_要件定義書.md
├─ 02_設計書.md
├─ 04_開発フェーズ計画書.md
├─ 05_Phase00実装仕様.md
├─ screen_mockup_2.html
├─ package.json
├─ src/
│  ├─ main/
│  ├─ preload/
│  ├─ renderer/
│  ├─ domain/
│  └─ data/
└─ tests/
   ├─ unit/
   └─ integration/
```

責務の境界を保ち、UIロジック、日報整形ロジック、データ永続化ロジックを分離する。Codexは既存構造がある場合に独断で大規模な再配置をせず、対象フェーズに必要な最小差分を優先する。

---

## 3. 画面設計

### 3.1 画面一覧

| 画面ID | 画面名 | 概要 |
|---|---|---|
| S-01 | メイン画面（日報作成） | 対象日の指定、作業ブロックの追加（同一案件の複数回追加可）、時系列表示 |
| S-02 | 案件管理画面 | 案件の新規登録・編集・削除・アーカイブ |
| S-03 | プレビュー/コピー画面 | 整形結果の確認とクリップボードコピー |
| S-04 | 過去日報一覧画面 | 日付ごとの過去日報の閲覧・再編集 |

### 3.2 データモデルの考え方（画面設計の前提）

要件（同一案件を1日に何度も行き来する）を踏まえ、UIの基本単位を「案件」ではなく「**作業ブロック**」とする。

- 1つの作業ブロック＝1つの案件×1つの時間帯×1つの業務内容
- 1日の日報は「作業ブロックのリスト」であり、同一案件のブロックが複数存在してよい
- 表示・出力時は、原則として**開始時刻の昇順**で自動的に並び替える（時系列＝入力順ではなく開始時刻順）

### 3.3 S-01: メイン画面（日報作成）

画面は「作業ブロックの時系列リスト」を主役に据える。案件タブで画面を切り替える方式ではなく、ブロックを縦に追加していくタイムライン形式とする。

```
┌──────────────────────────────────────────┐
│ 対象日: [2026/08/11 ▼]      [過去日報一覧]  │
├──────────────────────────────────────────┤
│  09:00-10:00 ● 案件A [MTG]        完了     │
│  ┌────────────────────────────┐          │
│  │ 作業内容: 要件確認MTG           │  [▲/▼並替]│
│  │ 進捗率: [100]%  詳細: (任意)     │  [削除]   │
│  └────────────────────────────┘          │
│                                            │
│  10:15-11:30 ● 案件B [資料作成]    進行中   │
│  ┌────────────────────────────┐          │
│  │ 作業内容: 提案資料のドラフト作成   │          │
│  │ 進捗率: [40]%                  │          │
│  └────────────────────────────┘          │
│                                            │
│  11:30-12:00 ● 案件A [資料作成]    進行中   │
│  ┌────────────────────────────┐          │
│  │ 作業内容: MTG議事録を設計書へ反映  │          │
│  │ 進捗率: [70]%                  │          │
│  └────────────────────────────┘          │
│                                            │
│        [＋ 作業ブロックを追加]              │
├──────────────────────────────────────────┤
│         [プレビューを表示 →]                │
└──────────────────────────────────────────┘
```

- 「＋ 作業ブロックを追加」を押すと、案件選択（登録済み案件から色付きチップで選ぶ、または新規登録）→ 業務種別ラベル・作業内容・進捗状況・進捗率・時間帯を入力する新規カードが末尾に追加される
- カードは開始時刻の入力／変更のたびに自動で時系列順に並び替わる（時刻未入力のブロックはリスト末尾固定）
- 各ブロックの左端の色ドット（●）で案件を判別できるようにする（3.6 UI/UXデザイン方針を参照）
- 業務種別ラベルは自由入力とする。ただし入力の手間を減らすため、過去に入力した値を候補としてオートコンプリート表示する（固定の選択肢は設けない）
- 手動並び替え（▲/▼）は、時刻未入力ブロック同士の順序調整など、自動ソートだけでは意図通りにならない場合の補助として用意する
- 入力内容は各フィールド変更時に自動でローカル一時保存（未完成の日報を保護）

### 3.4 S-03: プレビュー/コピー画面

```
┌──────────────────────────────────────────┐
│  プレビュー                                │
│ ┌────────────────────────────────────┐   │
│ │【日報】2026年08月11日（火）           │   │
│ │                                     │   │
│ │09:00-10:00 ■ 案件A（MTG）            │   │
│ │・作業内容：要件確認MTG                │   │
│ │・進捗状況：完了                       │   │
│ │                                     │   │
│ │10:15-11:30 ■ 案件B（資料作成）        │   │
│ │・作業内容：提案資料のドラフト作成       │   │
│ │・進捗状況：進行中（40%）              │   │
│ │                                     │   │
│ │11:30-12:00 ■ 案件A（資料作成）        │   │
│ │・作業内容：MTG議事録を設計書へ反映      │   │
│ │・進捗状況：進行中（70%）              │   │
│ └────────────────────────────────────┘   │
│                                            │
│      [クリップボードにコピー]                │
│      [＜ 入力に戻る]                        │
└──────────────────────────────────────────┘
```

- 同一案件のブロックが複数あっても、案件ごとにまとめずに時系列（発生順）のまま出力する（要件 FR-11）
- コピー成功時は「コピーしました」等のトースト通知を表示

### 3.5 時刻選択コンポーネント設計

- 開始時刻・終了時刻はそれぞれ「時」（00〜23）と「分」（00・05・10・…・55の5分刻み、12項目）を分けたプルダウンで選択する方式に確定した
- キーボード操作でも選択可能とする（矢印キーでの候補送り、タイプ入力での絞り込み）
- 終了時刻が開始時刻より前の場合は入力エラーとして視覚的に警告する
- 開始・終了の両方が選択されると、所要時間（時間:分）を自動計算し、隣に表示する
- 開始時刻を変更すると、リスト内でのブロックの並び位置がその場でアニメーション付きで入れ替わる（時系列順であることを視覚的にも伝える）

### 3.6 UI/UXデザイン方針

- 配色・タイポグラフィ・余白のルールを1つのデザイントークン（CSS変数等）にまとめ、全画面で統一する
- **案件ごとに固有の色を割り当て**、作業ブロックの左端ドット・案件チップ・プレビュー出力・過去日報一覧のタグなど、全画面を通じて同じ色で案件を判別できるようにする（同一案件が時系列上で離れた位置に複数回登場しても、色で瞬時に同じ案件と認識できることを狙う）
- 業務種別ラベル（MTG/資料作成/本番作業等）はテキストバッジとして小さく添え、案件色とは別軸の情報であることを視覚的に区別する
- 進捗率はプログレスバーまたは円形インジケーターなど、数値を読まなくても一目で進み具合がわかる表現を用いる
- ボタンやプルダウンなどのインタラクション要素にホバー/フォーカス時の軽いアニメーションを付け、操作の手応えを持たせる
- 詳細な配色・コンポーネントスタイルは実装フェーズ（後述フェーズ計画のUI/UXブラッシュアップフェーズ）でAIエージェントに設計・提案させ、都度レビューする

---

## 4. データ設計

### 4.1 保存先
OSごとのアプリデータ用ディレクトリ配下にJSONファイルとして保存する（Electronの `app.getPath('userData')` を想定）。

- Windows: `%APPDATA%\daily-report-app\`
- Mac: `~/Library/Application Support/daily-report-app/`

### 4.2 データ構造（案）

**案件マスタ（projects.json）**
```json
{
  "projects": [
    {
      "id": "proj-001",
      "name": "案件A",
      "code": "",
      "color": "#2f746d",
      "archived": false,
      "createdAt": "2026-07-01T00:00:00+09:00"
    }
  ]
}
```
- `color`: 案件を識別するための固有カラー（新規登録時に自動割当、後から変更可）

**日報データ（reports/2026-08-11.json）** — 「作業ブロック方式」

```json
{
  "date": "2026-08-11",
  "blocks": [
    {
      "id": "blk-001",
      "projectId": "proj-001",
      "projectName": "案件A",
      "taskLabel": "MTG",
      "workContent": "要件確認MTG",
      "status": "完了",
      "statusDetail": "",
      "progressPercent": 100,
      "startTime": "09:00",
      "endTime": "10:00",
      "note": ""
    },
    {
      "id": "blk-002",
      "projectId": "proj-002",
      "projectName": "案件B",
      "taskLabel": "資料作成",
      "workContent": "提案資料のドラフト作成",
      "status": "進行中",
      "statusDetail": "",
      "progressPercent": 40,
      "startTime": "10:15",
      "endTime": "11:30",
      "note": ""
    },
    {
      "id": "blk-003",
      "projectId": "proj-001",
      "projectName": "案件A",
      "taskLabel": "資料作成",
      "workContent": "MTG議事録・指摘事項を設計書へ反映",
      "status": "進行中",
      "statusDetail": "",
      "progressPercent": 70,
      "startTime": "11:30",
      "endTime": "12:00",
      "note": ""
    }
  ],
  "comment": "本日の所感・共有事項",
  "updatedAt": "2026-08-11T18:00:00+09:00"
}
```

- `blocks` 配列は保存順ではなく、表示・出力時に `startTime` の昇順でソートして扱う（`startTime` が空のブロックは配列末尾に固定するルールとする）
- 同一 `projectId` を持つブロックが複数存在すること（＝1日の中で同じ案件に何度も戻ること）を正式に許容するデータ構造とする
- 日報は日付単位で1ファイルに保存し、`reports/` フォルダ配下に蓄積する構成とする
- これにより過去日報一覧（S-04）はフォルダ内のファイル一覧を読み込むだけで実現できる

---

## 5. 整形（出力）ロジック設計

### 5.1 処理フロー
1. 対象日の `blocks` を取得し、`startTime` の昇順でソートする（`startTime` 未入力は末尾に）
2. テンプレート文字列に対して、日付・各ブロックの時間帯/案件名/業務種別/作業内容/進捗状況/進捗率等を差し込む
3. 末尾に所感（comment）があれば追記
4. 生成した文字列をプレビュー欄に表示し、クリップボードAPI（Electronの `clipboard.writeText`）でコピーする

### 5.2 出力テンプレート（デフォルト）
```
【日報】{YYYY年MM月DD日（曜日）}

{時系列順にソートされたblocksについて繰り返し}
{startTimeとendTimeがあれば"startTime-endTime "を先頭に付加}■ {projectName}{taskLabelがあれば「（" + taskLabel + "）」を付加}
・作業内容：{workContent}
・進捗状況：{status}{progressPercentがあれば「（" + progressPercent + "%）」を付加、さらにstatusDetailがあれば追記}

{comment があれば}
本日の所感・共有事項：{comment}
```

- 記号（■/・など）は設定画面でカスタマイズ可能な変数として持たせる（要件 FR-14 対応、将来拡張）
- 同一案件のブロックが複数回登場しても案件単位でまとめず、時系列の発生順のまま出力する

---

## 6. 主要な処理シーケンス

### 6.1 日報作成〜コピーまで
1. ユーザーがメイン画面で対象日を選択
2. 「＋ 作業ブロックを追加」で、案件（既存案件から選択 or その場で新規登録）・業務種別ラベル・作業内容・進捗状況・進捗率・時間帯を入力
3. 必要なだけ作業ブロックを追加（同一案件を何度追加してもよい）。ブロックは開始時刻順に自動で並び替わる
4. 「プレビューを表示」を押下 → 整形ロジックが時系列順にテキスト生成
5. 「クリップボードにコピー」を押下 → OSのクリップボードに格納
6. ユーザーがTeamsアプリ側で貼り付け・投稿（本アプリの処理範囲外）

---

## 7. エラーハンドリング／例外設計

| ケース | 対応 |
|---|---|
| 必須項目（対象案件・作業内容・進捗状況）未入力でプレビューを押下 | 該当ブロック・該当箇所をハイライトし、プレビューへ遷移しない |
| 進捗率に0〜100の範囲外の値が入力された | 範囲内に自動補正、または入力エラーとして警告する |
| 保存先フォルダへの書き込み権限がない | エラーダイアログを表示し、原因（権限不足）を明示 |
| 同一日付のデータが既に存在する場合の再編集 | 上書き確認ダイアログを表示してから保存 |
| クリップボードコピー失敗（OS側の制約等） | エラーメッセージを表示し、手動選択コピー用にプレビューのテキストを選択状態にする |

---

## 8. セキュリティ設計・検証方法

- **通信遮断の静的確認**: `package.json` とソースを棚卸しし、通信系ライブラリおよび `http://` / `https://` / `fetch(` / `axios` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` 等を確認する
- **外部リソース確認**: HTML/CSS/JSにGoogle Fonts、CDN、外部画像、外部スクリプト参照がないことを確認する
- **Electron設定確認**: `contextIsolation: true`、`nodeIntegration: false` を確認し、preloadの公開APIが必要最小限であることをレビューする
- **通信遮断の動的確認**: ビルド済みアプリを、OSのファイアウォールでアプリ単位の通信を完全ブロックした状態、またはネットワーク未接続のPCで一通り操作し、正常に動作することを確認する
- **テストデータ**: 実在の顧客・案件・業務情報は使用せず、ダミーデータのみで検証する
- **配布物の完全性**: 実行ファイルはローカルで生成・手渡し配布し、社内ポリシーに反する外部自動配布サービスは使用しない
- **ログ設計**: デバッグログに業務内容の具体的テキストを出力しない。必要なログはローカル限定かつ機密情報を含めない
- **回帰確認**: 各フェーズ完了時に対象機能の自動テストを実行し、既存テストも可能な範囲で再実行する


---

## 9. 今後の検討事項
- 過去日報データの保存件数上限・自動アーカイブ方針
- 出力フォーマットのカスタマイズ項目の詳細（案件ごとに絵文字を変えたい等の要望有無）
- Windows/Mac向けビルド・配布の具体的な手順書（別途「配布・運用手順書」として作成推奨）
