# 開発フェーズ計画書 — Codex版

## 1. 目的
CodexによるAI駆動開発では、1回のタスク範囲を小さく保ち、仕様・実装・検証を対応付ける。
本プロジェクトでは、**1フェーズ＝1つの完結した作業単位＝1つのGitブランチ候補**として管理する。

各フェーズは「実装した」ことではなく、**完了条件を検証できたこと**をもって完了とする。

## 2. Codexでの進め方
- 原則1つのCodexタスクで1フェーズ
- `AGENTS.md` → `01_要件定義書.md` → `02_設計書.md` → 本書の対象フェーズを確認
- 実装前に対象コードと既存テストを調査
- スコープ外の機能追加・大規模リファクタリング禁止
- 実装後にテスト、通信禁止チェック、差分確認
- 完了条件を `PASS / FAIL / NOT VERIFIED` で評価
- `FAIL` または重要な `NOT VERIFIED` があれば完了扱いにしない
- commit / tag / merge はユーザーが明示依頼した場合のみ実行

## 3. Gitブランチ運用
- `main`: 常に動作可能
- `feature/phaseNN-<内容>`
- レビュー・動作確認後に `main` へマージ
- 完了タグ: `v0.NN`
- コミット: `[Phase NN] <内容>`
- 通常は実装・検証・コミット案提示まで。勝手にcommitしない

## 4. フェーズ一覧

| # | フェーズ名 | スコープ | Codexでの主な検証 | 完了条件 |
|---|---|---|---|---|
| 00 | プロジェクト初期化 | `05_Phase00実装仕様.md` に従いElectron + TypeScript + Forge + Webpackの土台を構築 | `typecheck`、Vitest、network check、package/make、起動確認 | `05_Phase00実装仕様.md` 15章をすべて満たす。Win/Mac実機の未確認側はNOT VERIFIED、両OS最終確認はPhase09 |
| 01 | データ層 | 案件・日報JSONのCRUD | unit test、temp directory save/load、異常系 | CRUDとsave/loadが自動テストで確認できる |
| 02 | 案件管理画面（S-02） | 登録・編集・削除・アーカイブUI | UI操作、data層統合 | UI操作が案件マスタへ正しく反映 |
| 03 | 日報入力画面 基本（S-01） | 対象日、block追加/削除/順序、内容・進捗、自動保存 | 複数block、同一案件複数回、reload | 複数blockの入力・保存・復元が確認できる |
| 04 | 時刻選択UI | 時/分、5分刻み、所要時間、error、自動sort | 境界値、未入力、同時刻、終了<開始 | 時刻選択・計算・警告・sortが仕様通り |
| 05 | 整形・プレビュー（S-03） | 日報テキスト生成と表示 | string/snapshot test、往復ケース | 設計書5章どおり、同一案件をまとめず時系列出力 |
| 06 | クリップボードコピー | copyとfeedback | clipboard境界mock、手動確認 | 正しいtextをcopy、成否がUIで分かる |
| 07 | 過去日報一覧（S-04） | 日付一覧、再閲覧・再編集 | 複数日JSON、異常データ | 選択・閲覧・再編集ができる |
| 08 | UI/UXブラッシュアップ | token、配色、typography、animation、validation | 4画面目視、keyboard/focus | 全画面で一貫性とfeedback |
| 09 | 通信遮断・パッケージング | Win/Mac成果物、offline検証 | static grep、firewall/offline、各OS起動 | offlineで全機能動作、Win/Mac配布物を実機確認 |

## 5. フェーズ依存
```text
00 → 01 → 02
      └→ 03 → 04 → 05 → 06 → 07 → 08 → 09
```
同じファイルを複数Codexタスクで同時編集しない。

## 6. Codexへのフェーズ開始プロンプト
```text
Phase <NN>「<フェーズ名>」を実装してください。

最初に以下を読んでください。
- AGENTS.md
- 01_要件定義書.md
- 02_設計書.md
- 04_開発フェーズ計画書.md の Phase <NN>
- 対象フェーズの個別実装仕様（Phase00では 05_Phase00実装仕様.md）
- 必要に応じて screen_mockup_2.html

進め方:
1. スコープ・完了条件と既存コード/テストを確認
2. 変更予定ファイルと検証方法を簡潔に整理
3. スコープ内だけを最小差分で実装
4. 必要な自動テストを追加・更新
5. テストと通信禁止の静的チェック
6. git diffを確認
7. 完了条件を PASS / FAIL / NOT VERIFIED で報告
8. スコープ外改善案は実装せずメモ
9. コミットは行わず、コミットメッセージ案だけ提示

重要:
- AGENTS.md のネットワーク通信禁止・機密情報ルールを最優先
- 実在顧客・案件データをテストに使わない
- 不明点があっても要件を勝手に拡張しない
```

## 7. レビュー専用プロンプト
```text
現在の差分を Phase <NN> の実装としてレビューしてください。
コードはまだ変更せず、まず問題点だけを重要度順に報告してください。

参照:
- AGENTS.md
- 01_要件定義書.md
- 02_設計書.md
- 04_開発フェーズ計画書.md の Phase <NN>

確認観点:
- 要件漏れ・仕様逸脱
- スコープ外変更
- バグ・境界値
- Electron IPC / セキュリティ
- 外部通信・外部リソース
- 機密情報のログ
- テスト不足
- 保守性
```

## 8. Phase完了時チェック
```text
[ ] 完了条件を満たした
[ ] 新規/変更ロジックのテストを実行
[ ] 既存テストを壊していない
[ ] 外部URL・通信API・通信系依存を追加していない
[ ] 実在顧客/案件データをコード・テスト・ログに入れていない
[ ] git diffでスコープ外変更がない
[ ] 未検証事項をNOT VERIFIEDと明記
[ ] コミットメッセージ案を提示
```

## 9. 今後の検討事項
- Phase09のWindows/Mac実機検証手順の別紙化
- UI回帰確認を画像比較で行うか
- 過去日報保存件数・自動アーカイブ方針
