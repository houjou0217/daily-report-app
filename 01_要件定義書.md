# 日報生成アプリ 要件定義書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | 日報生成アプリ 要件定義書 |
| バージョン | 1.2 (Codex向け開発方針反映) |
| 作成日 | 2026-08-11 |

---

## 2. 目的・背景

- 業務は複数の案件を並行して進めており、1日の中で複数案件の作業が発生する。さらに、1つの案件の中でも「MTG」「資料作成」「本番作業」など複数の業務が発生し、他案件の対応を挟んで同じ案件の作業に何度も戻ってくる（往復する）ことがある。
- 日報は Microsoft Teams に投稿する運用となっている。
- 業務内容が銀行・行政等のセキュリティ・リテラシー要求が高い顧客に関わるため、日報の入力内容がネットワーク経由で外部に漏洩することを一切許容できない。
- そのため、**ネットワーク通信を一切行わないローカル完結型のアプリ**として、1日の中で発生した作業を時系列の単位で入力すると、時系列・案件区分・進捗が読み手にわかりやすい形に自動整形してくれるツールを作成する。

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| 案件 | 並行して進めている業務・プロジェクトの単位 |
| 作業ブロック | 1つの案件に対する、ある時間帯の作業のまとまり（例: 「案件Aの9:00-10:00のMTG」）。1日の中で同一案件の作業ブロックを複数回記録できる |
| 日報 | 1日の作業ブロックをまとめた報告文書 |
| 整形 | 入力された作業ブロックを、Teamsにそのまま貼り付け可能な体裁のテキストに変換する処理 |
| ローカル完結 | アプリの起動から日報生成までの全処理がネットワーク通信を行わないこと |

## 4. 利用者・利用シーン

- **利用者**: 複数案件を掛け持ちする担当者本人（1台のPCで日常的に使用）
- **利用シーン**:
  1. 業務終了時にアプリを起動する
  2. 本日行った作業を「作業ブロック」として時間の発生順（または思い出した順）に追加していく。1つの作業ブロックには対象案件・業務内容・時間帯・進捗状況・進捗率を入力する。同じ案件の作業を1日に何度追加してもよい
  3. すべての作業ブロックを入力し終えたら、整形済みテキストをプレビューする（開始時刻順に自動で並び替えられる）
  4. 「コピー」操作で整形済みテキストをクリップボードに格納する
  5. Teamsに手動で貼り付けて投稿する

## 5. スコープ

### 5.1 対象範囲（スコープ内）
- 複数案件・複数作業ブロックの日報入力・整形・クリップボードへのコピー
- 案件のローカルでの登録・編集・削除・選択
- 過去の入力内容のローカル保存・呼び出し（案件名の再利用、前回入力の参照など）
- Windows / Mac 両OSでの動作、インストール不要（実行ファイル起動のみ）

### 5.2 対象外（スコープ外）
- Microsoft Teams への自動投稿・API連携（ネットワーク通信を伴うため対象外）
- クラウド同期・バックアップ機能
- 複数ユーザーでのデータ共有・マルチアカウント対応
- モバイル対応

## 6. 機能要件

### 6.1 案件管理機能
- FR-01: 案件を新規登録できる（案件名、任意で案件コード・分類等）
- FR-02: 登録済み案件の一覧から選択できる
- FR-03: 案件情報を編集・削除できる
- FR-04: 「今日はもう使わない」案件をアーカイブ（非表示化）できる（削除とは別に、一覧を圧迫しないため）

### 6.2 日報入力機能（作業ブロック方式）
- FR-05: 対象日（デフォルトは当日、変更可能）を指定できる
- FR-06: 1日の中で「作業ブロック」を必要な数だけ追加できる。1つの作業ブロックは1つの案件に紐づくが、**同一案件の作業ブロックを1日に何度でも追加できる**（例: 午前に案件AでMTG → 午後に案件Bで資料作成 → 夕方に再び案件Aで対応、という往復を記録できる）
- FR-07: 各作業ブロックには以下を入力できる
  - 対象案件（必須、選択式）
  - 業務種別ラベル（任意／自由入力。例: MTG、資料作成、本番作業）
  - 作業内容（必須、自由記述）
  - 進捗状況区分（必須／進行中・完了・保留などの区分）
  - **進捗率（任意／0〜100%の数値で入力。案件全体ではなく、その作業ブロック時点での体感進捗を表す）**
  - 開始時刻・終了時刻（「時」「分」それぞれプルダウンで選択。分は5分間隔／任意入力。選択すると所要時間を自動計算して表示する）
  - 特記事項・課題・相談事項（任意）
- FR-08: 作業ブロックは、**開始時刻をもとに自動で時系列順に並び替えて**表示・出力される（開始時刻が未入力のブロックは入力順で末尾に表示するなど、扱いを定義する）
- FR-09: 開始時刻が同一のブロックが複数ある場合や時刻未入力の場合に備え、手動でも順序を調整できる
- FR-10: 入力途中の内容は自動保存され、アプリを閉じても失われない

### 6.3 整形・出力機能
- FR-11: 入力内容から、**時系列順**に並んだ、案件区分・業務種別・進捗（状況＋進捗率）がわかる読みやすいテキストを自動生成する。同一案件の作業ブロックが複数回登場する場合も、まとめずに発生順のまま出力する
- FR-12: 出力される日報には、少なくとも「日付」「時間帯」「案件名」「作業内容」「進捗状況」が明確に区分される形式で含まれ、進捗率が入力されていればあわせて表示する
- FR-13: 整形済みテキストをワンクリックでクリップボードにコピーできる
- FR-14: 整形フォーマット（見出しの記号や区切り線など）をある程度カスタマイズできる（任意要件）

### 6.4 履歴・保存機能
- FR-15: 日報はローカルに保存され、日付ごとに一覧・再閲覧・再編集できる
- FR-16: 過去の同一案件の入力内容をテンプレートとして呼び出せる（入力の手間軽減）
- FR-17: データはすべてローカルファイル（例: PC内のアプリデータフォルダ）に保存する

## 7. 非機能要件

### 7.1 セキュリティ・通信要件（最重要）
- NFR-01: アプリはいかなる自動アップデートチェック・外部API通信・テレメトリ送信も行わない
- NFR-02: アプリのビルドに、ネットワーク通信を行うライブラリ・SDK（分析ツール、クラッシュレポート送信等）を含めない
- NFR-03: 入力データは暗号化なし平文でも良いが、少なくとも外部への自動送信経路が存在しないことを保証する（ローカルファイルとしてのみ存在）
- NFR-04: アプリの動作ログに、入力された業務内容の詳細（案件名・作業内容等の機密情報）を出力しない、または出力する場合もローカル保存のみとする

### 7.2 可搬性・動作環境
- NFR-05: Windows / Mac の両OSで動作すること
- NFR-06: インストール作業（管理者権限でのセットアップ等）を必要とせず、実行ファイルを起動するだけで使用できること
- NFR-07: 社内PC等、制限された環境（インターネット未接続、管理者権限なし等）でも動作すること

### 7.3 ユーザビリティ
- NFR-08: 1日分の複数作業ブロックの入力から整形・コピーまでを数分以内に完了できる操作性であること
- NFR-09: 読み手（Teamsで日報を見る他のメンバー）が、どの時間帯にどの案件のどんな作業をどこまで進めたかをひと目でわかる出力であること
- NFR-10: 同一案件を1日に何度も行き来する入力スタイルでも、案件の追加・切替の手間が増えすぎないこと（案件選択のクリック数を最小限にする）

### 7.4 保守性
- NFR-11: 出力フォーマットのテンプレートは将来変更しやすい構造にする

### 7.5 UI/UX品質
- NFR-12: 単に機能が動くだけでなく、視覚的な洗練さ・操作の気持ちよさにこだわる（配色・タイポグラフィ・余白・アニメーションの統一感）
- NFR-13: 時刻選択などの頻出操作は、クリック数・入力の手間が最小になるよう設計する（例: 開始/終了時刻はプルダウンで即選択、自由入力によるフォーマット崩れを防ぐ）
- NFR-14: 入力ミスや必須項目の未入力に対して、その場で分かりやすいフィードバック（色・アイコン・メッセージ）を返す

## 8. 制約条件
- ネットワーク通信を一切行わないこと（最上位の制約）
- Teamsへの投稿は手動貼り付けとする（自動連携は行わない）
- 開発・配布物にネットワーク通信を要するランタイムのインストール手順を含めない

## 9. 想定される日報出力イメージ（イメージ例）

同一案件（案件A）に複数回戻る、案件をまたいだ往復のケースを含む例:

```
【日報】2026年08月11日（火）

09:00-10:00 ■ 案件A（MTG）
・作業内容：要件確認MTG
・進捗状況：完了

10:15-11:30 ■ 案件B（資料作成）
・作業内容：提案資料のドラフト作成
・進捗状況：進行中（40%）

11:30-12:00 ■ 案件A（資料作成）
・作業内容：MTG議事録・指摘事項を設計書へ反映
・進捗状況：進行中（70%）

13:00-15:00 ■ 案件B（本番作業）
・作業内容：本番環境へのリリース作業
・進捗状況：完了

本日の所感・共有事項：特になし
```

## 10. 開発方針（CodexによるAI駆動開発）

本プロジェクトは OpenAI Codex を主要なAIコーディングエージェントとして利用し、仕様・実装・検証の認識齟齬を抑えながら段階的に開発する。

- `AGENTS.md`: Codexが常時参照するプロジェクト規約・禁止事項・検証ルール
- `04_開発フェーズ計画書.md`: 画面・機能単位でのフェーズ分割、Git運用、各フェーズの完了条件
- `05_Phase00実装仕様.md`: 初期技術スタック、セキュリティ設定、テスト・ビルド基準
- `screen_mockup_2.html`: UIの視覚的な参照資料。要件・設計書と矛盾する場合は、要件定義書 → 設計書 → 開発フェーズ計画書 → モックの順で優先する

### 10.1 Codexへのタスク付与原則
- 1回のタスクで扱う範囲は原則1フェーズに限定する
- Codexは着手前に `AGENTS.md`、本要件定義書、設計書、対象フェーズのスコープと完了条件を確認する
- 実装前に変更予定ファイルと検証方法を簡潔に整理し、その後に実装する
- スコープ外の仕様変更・機能追加は実装せず、「今後の検討事項」として報告する
- 実装後はテスト、静的確認、差分確認を行い、完了条件との対応を報告する
- 既存仕様に曖昧さがあっても、セキュリティ最上位制約を弱める推測は行わない

### 10.2 AI利用時の機密情報ルール
- Codexへのプロンプト、テストデータ、スクリーンショット、fixture、ログには、実在顧客名・案件名・業務内容・個人情報・認証情報を使用しない
- 開発・テストでは `案件A`、`案件B` 等のダミーデータのみを用いる
- 本番利用時に入力される日報データをAIサービスへ送信する機能は実装しない


## 11. 今後の確認事項（設計書作成前に要合意）
- 出力フォーマットの細かい記号（■/・/罫線など）の指定有無
- 過去日報の保存期間・件数の上限
